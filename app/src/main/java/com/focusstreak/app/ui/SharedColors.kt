package com.focusstreak.app.ui

import androidx.compose.ui.graphics.Color

// Shared icon background / tint colors used by multiple screens so we don't
// get "private in file" visibility errors when the same color is needed in
// both light and dark themed screens.

internal val IconBgOrange = Color(0xFFFFE0B2)
internal val IconTintOrange = Color(0xFFFF9800)
